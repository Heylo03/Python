ARRANCAR XAMPP:
sudo docker run -p 8080:80 -v /home/alumnom/IAW:/www -d tomsik68/xampp:8

vagrant box list
vagrant box add ubuntu/xenial64 (AÑADE EL SO A VAGRANT)
vagrant up (ARRANCA LA MAQUINA)
Vagrant halt
vagrant destroy -f
vagrant box list
vagrant status {maquinavirtual}
vagrant ssh {maquinavirtual}
    ls /vagrant
set -xeu
ss -tlpn (VER SERVICIOS)


ip link
sudo dhclient enp3s0
sudo dhclient -r enp3s0